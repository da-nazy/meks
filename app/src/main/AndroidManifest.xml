<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.meks_enginering">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- // Adding Read External Storage Permission -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".user.verification"></activity>
        <activity android:name=".order.order_process" />
        <activity android:name=".order.order_details" />
        <activity android:name=".menu.menu" />
        <activity android:name=".admin.create_employee" />
        <activity android:name=".requests.pendingRequests.pendingRequests" />
        <activity android:name=".requests.completedRequests.completedRequest" />
        <activity
            android:name=".admin.admin"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".user.profile" />
        <activity android:name=".user.get_started" />
        <activity android:name=".user.forgot_password" />
        <activity android:name=".user.sign_in" />
        <activity android:name=".user.sign_up" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>